Using only the MCP Neo4j tools and by consulting the schema here '/workspaces/code-continuum/doc/SCHEMA_IA.md', give me the complete sequence diagram of the Rust function $ARGUMENTS. The call graph must be explored at maximum depth (traverse all CALLS edges recursively until leaf functions with no further outgoing calls). Every callee at every level must appear as a participant and every call edge must be represented in the diagram.
