// Ce fichier n'est plus inclus comme module (supprimé de java/mod.rs).
// Les relations EXTENDS/IMPLEMENTS sont gérées par DslExecutor::resolve_extends_implements_global()
// dans dsl_executor/mod.rs, après extraction de tous les fichiers.
